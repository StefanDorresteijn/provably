//Created by Stefan on 9/28/2016.

extends ../layout

block content
    h1
        = drawing.name
        small.lottery-state &nbsp;(#{drawing.state})
    if drawing.state == "finished"
        p
            | Ended on&nbsp;
            = strftime('%B %d, %Y', drawing.endTime)
    else if drawing.state == "closed"
        p Calculating winner(s)
    else
        p
            | Ending on&nbsp;
            = strftime('%B %d, %Y', drawing.endTime)

    if drawing.state == "finished"
        h3
            | Winners
            small &nbsp;(#{drawing.winners.length})
        ul.list-group
            each winner in drawing.winners
                li.list-group-item= winner
    #entry_list(data-id=drawing._id)